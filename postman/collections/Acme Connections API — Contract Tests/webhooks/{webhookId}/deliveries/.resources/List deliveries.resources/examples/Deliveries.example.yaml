$kind: http-example
request:
  url: "{{baseUrl}}/webhooks/:webhookId/deliveries?status=failed"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    status: failed
  pathVariables:
    webhookId: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
