$kind: http-example
request:
  url: "{{baseUrl}}/partners?tier=standard"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    tier: standard
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
